<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.donuts">

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Donuts"
        tools:replace="android:theme">
        <activity android:name=".OrderCompletePage"></activity>
        <activity android:name=".InventoryUpdatePage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".CheckoutPage" />
        <activity android:name=".ShoppingCartPage" />
        <activity android:name=".OrderPage" />
        <activity android:name=".MenuOptionsPage" />
        <activity android:name=".UserPortal" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>